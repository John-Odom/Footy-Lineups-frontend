import React from 'react'

const Avatar = (props) => {
        return(
            <div id="profile-image">
                <p>Avatar: {props.avatar}</p>
            </div>
        )
    }
export default Avatar